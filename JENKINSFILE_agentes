pipeline {
    // Flujo principal en el Master para liberar a agent2
    agent none 

    options { skipDefaultCheckout() }

    stages {
        stage('Get Code') {
            // Asignar al Master (Windows)
            agent { label 'master' } 
            steps {
                git 'https://github.com/JulioEguis/helloworld.git'
                echo WORKSPACE
                
                // CORRECCIÓN: Usar bat en Windows
                bat 'dir' 
                
                stash name:'code', includes:'**'
            }
        }
    
        stage('Build') {
            // Asignar al Master (Windows)
            agent { label 'master' } 
            steps {
                echo 'Eyyy, esto es Python. No hay que compilar nada!!!'
            }
        }
        
        stage('Tests')
        {
            parallel
            {
                stage('Unit') {
                    agent {label 'agent1'}
                    steps {
                        catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
                            unstash name:'code'
                            sh '''
                                ls -la
                                export PYTHONPATH=${WORKSPACE}
                                /usr/bin/pytest --junitxml=result-unit.xml test/unit
                            '''
                            stash name:'unit-res', includes:'result-unit.xml'
                        }
                    }
                }   
                
                stage('Rest') {
                    agent {label 'agent2'}
                    steps {
                        catchError(buildResult: 'UNSTABLE', stageResult: 'FAILURE') {
                            unstash name:'code'
                            sh '''
                                export FLASK_APP=app/api.py
                                
                                /usr/bin/flask run & 

                                sleep 4

                                java -jar /home/agent2/wiremock/wiremock-standalone-3.3.1.jar --port 9090 --root-dir /home/agent2/wiremock &
                                
                                export PYTHONPATH=${WORKSPACE}
                                
                                sleep 15
                                
                                /usr/bin/pytest --junitxml=result-rest.xml test/rest
                            '''
                            // El stash se ejecuta solo si el pytest tuvo éxito
                            stash name:'rest-res', includes:'result-rest.xml'
                        }
                    }   
                }       
            }
        }

        stage('Results') {
            // Asignar al Master (Windows)
            agent { label 'master' } 
            steps {
                unstash name:'unit-res'
                // Esta línea sólo funcionará si Rest terminó y guardó el stash
                unstash name:'rest-res' 
                junit 'result*.xml' 
            }
        }
    }
}
